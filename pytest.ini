[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --cov=instagram_schemas --cov-report=term-missing
markers =
	integration: integration tests using real API fixture data in tests/data
	mongodb: tests for MongoDB serialization compatibility